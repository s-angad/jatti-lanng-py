name: CI

on:
  push:
  pull_request:

jobs:
  build_windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      # No build or sample run needed; jatti.exe is committed for out-of-box usage
